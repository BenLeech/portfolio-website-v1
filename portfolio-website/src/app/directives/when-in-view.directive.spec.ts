import { WhenInViewDirective } from './when-in-view.directive';

describe('WhenInViewDirective', () => {
  it('should create an instance', () => {
    const directive = new WhenInViewDirective();
    expect(directive).toBeTruthy();
  });
});
